import { createContext } from 'react';

import { FlowDataContextType } from '../types';

// ----------------------------------------------------------------------

export const FlowDataContext = createContext({} as FlowDataContextType);
